name: Hellow World
on: workflow_dispatch
jobs:
  hello-job:
    strategy:
      matrix:
        os: [ubuntu-latest]
    runs-on: ${{matrix.os}}
    steps:
    - name: Echo text
      run: echo "Hello, world!"